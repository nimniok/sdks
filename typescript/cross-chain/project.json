{
  "name": "cross-chain",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "typescript/cross-chain/src",
  "projectType": "library",
  "tags": ["typescript", "sdk"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "typescript/cross-chain",
        "commands": ["tsdown"]
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "passWithNoTests": true,
        "coverage": true,
        "reportsDirectory": "../../coverage/typescript/cross-chain"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["typescript/cross-chain/**/*.ts"]
      },
      "outputs": ["{options.outputFile}"]
    },
    "type-check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p typescript/cross-chain/tsconfig.lib.json"
      }
    }
  }
}
